'use strict';

// Angular
import { NgModule, ModuleWithProviders } from '@angular/core';

// Service Collection
let imports: any[] = [
];

let providers: any[] = [
];

let declarations: any[] = [

];

// NgModule
@NgModule({
    imports: imports,
    providers: providers,
    declarations: declarations,
    exports: declarations
})
export class IvyAngular<%= name %>Module {
}